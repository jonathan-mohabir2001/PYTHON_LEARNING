# This python file will be practise of formatting numbers. 
x = 3.0 
b = -1234
y = 17 
z = 34.12344

print(abs(b)); 
# the abs format above turns the negative b variable into a postive number. 
print(float(y)); 
# the float format above turns the integer number into a decimal number. 
print(round(z,3)); 
#the round format above rounds the decimal number to 2 decimal places. Second parameter 
# passed when using the round method will specifcy the number of decimal places. 
print(str(y)); 

